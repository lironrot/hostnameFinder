# Container hostname Finder

This docker compose will tell you from localhost web, what is the hostname of container.

Step one - clone the repo to your machine.
Step two - get into the directory you cloned the repo and type "docker-compose up -d" (will run by background)
Step tree - open browser and type the net url -  http://localhost:5000/
than you will see the host name of the container
